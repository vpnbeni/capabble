# SCHOL API (school-intelligence FastAPI)
VITE_SCHOL_API_URL=http://localhost:8001

# Vite dev server port (default 5175)
# VITE_SCHOL_PORT=5175

# Local app URLs (after npm run dev:schol from repo root):
#   http://schol.capabble.localhost:5175   (recommended — no hosts file)
#   http://schol.capabble:5175             (requires scripts/setup-local-host.ps1 on Windows)
